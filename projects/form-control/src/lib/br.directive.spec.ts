import { BrDirective } from './br.directive';

describe('BrDirective', () => {
  it('should create an instance', () => {
    const directive = new BrDirective();
    expect(directive).toBeTruthy();
  });
});
