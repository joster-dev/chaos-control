import { BorderRadiusDirective } from './border-radius.directive';

describe('BorderRadiusDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderRadiusDirective();
    expect(directive).toBeTruthy();
  });
});
